import { Grid, Box, Typography } from "@mui/material";
import Reac from "react";

const AHP = () => {
  return (
    <Grid container columns={12}>
      <Grid item xs={12}>
        <Box>
          <Typography>Perhitungan Menggunakan metode ahp</Typography>
        </Box>
      </Grid>
    </Grid>
  );
};

export default AHP;
